#!name=Xbox Cart Trigger
#!desc=访问 add_to_cart.com 时执行 Xbox 加购脚本
#!category=XBOX

[General]
# 强制让 Surge 处理这个假域名的 HTTP 请求
force-http-engine-hosts = %APPEND% add_to_cart.com

[Script]
# 注意：script-path 需要指向你实际保存 JS 文件的路径
Xbox_Add_Cart = type=http-request, pattern=^http://add_to_cart\.com/?$, script-path=NewAddToCart_Web.js, timeout=60

[MITM]
# 只需要拦截 HTTP，不需要 HTTPS 解密，因为这是个假域名
hostname = %APPEND% add_
